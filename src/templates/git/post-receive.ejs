#!/bin/sh
GIT_WORK_TREE=<%-appDir %> git checkout -f
echo received new push
pwd
pwd=$(pwd)
while read oldrev newrev refname
do
    branch=$(git rev-parse --symbolic --abbrev-ref $refname)
    if [ "master" = "$branch" ]; then
        cd <%-appDir %>
        npm init
        cd $pwd
    fi
done